<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:paddingTop="5dp"
    android:paddingBottom="10dp"
    tools:context=".confirmationDialog.ConfirmationDialog">

    <TextView
        android:id="@+id/title"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:padding="15dp"
        android:textColor="?colorOnSurface"
        android:textSize="20sp"
        android:textStyle="bold"
        tools:text="Delete Post ?" />

    <TextView
        android:id="@+id/content"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="20dp"
        android:alpha="0.6"
        android:gravity="center"
        android:padding="15dp"
        android:textColor="?colorOnBackground"
        tools:text="If you don't want to delete this post, you can archive it. Only you'll be able to set it." />

    <View
        android:layout_width="match_parent"
        android:layout_height="0.1dp"
        android:alpha=".8"
        android:background="?colorOnSurface" />

    <TextView
        android:id="@+id/positive_action"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:padding="15dp"
        android:textColor="?colorPrimary"
        android:textStyle="bold"
        tools:text="Positive Action" />

    <View
        android:layout_width="match_parent"
        android:layout_height="0.1dp"
        android:alpha=".8"
        android:background="?colorOnSurface" />

    <TextView
        android:id="@+id/negative_action"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:padding="15dp"
        android:textColor="?colorPrimary"
        tools:text="Negative Action" />

</LinearLayout>